name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  test-and-scan:
    name: Test / Lint / Scan
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11, 3.12]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml','**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        # Assumes you have a pyproject.toml with optional "dev" extras
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev] || pip install -e .

      - name: Lint with ruff
        # Install ruff in dev deps or run via pip
        run: |
          if ! command -v ruff >/dev/null 2>&1; then pip install ruff; fi
          ruff check src tests || true
        # Note: keep exit code 0 here initially to prevent CI failure while iterating.
        # Remove "|| true" to make lint blocking once rules are stable.

      - name: Run tests
        run: |
          pytest -q

      - name: Run scanner (text report)
        id: run_text
        run: |
          mkdir -p ci-reports
          # run the CLI on the example folder; adjust path as needed
          agent-scan examples/demo-agent > ci-reports/agent-scan-report.txt || true
          echo "::set-output name=text_report::ci-reports/agent-scan-report.txt"

      - name: Run scanner (json report)
        id: run_json
        run: |
          mkdir -p ci-reports
          # JSON output if you implement "--json" flag
          agent-scan examples/demo-agent --json > ci-reports/agent-scan-report.json || true
          echo "::set-output name=json_report::ci-reports/agent-scan-report.json"

      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: agent-scan-reports-py${{ matrix.python-version }}
          path: ci-reports/

      - name: Show short summary (for logs)
        run: |
          echo "==== short summary of text report ===="
          head -n 40 ci-reports/agent-scan-report.txt || true